apiVersion: atmos/v1
kind: AtmosVendorConfig
metadata:
  name: ec2-client-vpn
  description: Vendor dependencies for local development and linting
spec:
  sources:
    - component: "account-map"
      source: github.com/cloudposse-terraform-components/aws-account-map.git//src?ref={{.Version}}
      version: main
      targets:
        - "account-map"
      included_paths:
        - "**/**"
      excluded_paths: []
